FROM python:3.11-slim

WORKDIR /app
COPY ml-workers/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ml-workers/ .

EXPOSE 8001
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]
